#!/usr/bin/env bash
set -eu

################################################################################
# Get SSH Key from GitHub
################################################################################
_git_key() {
  local gituser=${1:-$(git config --get github.user)}
  curl "https://github.com/${gituser}.keys"
}

_git_key "$@"
