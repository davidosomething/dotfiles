#!/usr/bin/env zsh

# Repair brew repo via hard reset

[[ "$OSTYPE" == *'arwin'* ]] &&
  (( $+commands[brew] )) &&
  cd -- "$(brew --repo)" &&
  git fetch &&
  git reset --hard origin/master &&
  brew update
