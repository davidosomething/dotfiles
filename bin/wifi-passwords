#!/usr/bin/env bash

# Get plaintext saved wifi passwords stored by NetworkManager

[[ -d /etc/NetworkManager/system-connections/ ]] && \
  sudo grep -H '^psk=' /etc/NetworkManager/system-connections/*

