# ~/.dotfiles/zsh/.zshrc.local.osx
# sourced by .zshrc

# add my custom zsh completions
fpath=( ~/.dotfiles/zsh-completions $fpath )

# rbenv completions (can also add to bash, but it's not there)
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
