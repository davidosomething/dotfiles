# ----------------------------------------------------------------------------
# Functions
# ----------------------------------------------------------------------------

# terse zsh-specific up
up() {
  local limit=1
  local d=""
  [[ $1 =~ '^[0-9]+$' ]] && limit=$1
  while ((limit--)); do d="../${d}"; done
  cd "$d"
}
