<?php
require 'php2.php';

$something = [
    'a' => 1,
    'b' => 2,
];

foreach ($a as $k => $v) {
    echo "$a";
}

$n = $something[1];

/**
 * @param Integer $number
 * @return Integer
 */
function glbfunc(int $number)
{
    return $something[1];
}

function elb()
{
    $inst = new MyNs\Mine();

    // =======================================================================
    // Type anything off of $inst to get padawan completion
    // =======================================================================
    //
    // e.g.
    // $inst->
    // $inst::
    ?>
    <p>testing</p>
    <!-- the script tag will match as jsOperator in bad vim-javascript -->
    <script type="text/javascript">
    const b = 'hi';
    </script>
    <?php
}
