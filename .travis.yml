---
sudo: false
language: python
os:
  - linux
python:
  - '2.7'
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y shellcheck
  - nvm install --lts
script:
  - tox
  - npm test --silent
deploy:
  provider: script
  script: meta/travis-automerge.bash
  "on":
    branch: dev
