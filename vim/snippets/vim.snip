# This already exists in neosnippet-snippets but uses shortform of set cpo&vim
# and cpo_save is called save_cpo. I just prefer this style instead:
snippet  cpoptions
abbr     let s:cpo_save = &cpooptions | set cpoptions&vim
alias    s:cpo_save cpoptions
options  head
    let s:cpo_save = &cpoptions
    set cpoptions&vim

    ${0}

    let &cpoptions = s:cpo_save
    unlet s:cpo_save
