################################################################################
# OSX specific .tmux.conf

# set default shell PROPERLY and allow pbcopy/pbpaste  --  BROKEN
set -g default-command "reattach-to-user-namespace -l \"$SHELL\""
